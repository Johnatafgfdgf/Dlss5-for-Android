#version 450
layout(local_size_x=64) in;
layout(set=0,binding=0) buffer Data { uint data[]; };
layout(push_constant) uniform Params { uint count; uint addValue; } pc;
void main() {
    uint i=gl_GlobalInvocationID.x;
    if(i<pc.count) data[i]+=pc.addValue;
}
